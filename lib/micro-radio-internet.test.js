'use strict'
/* eslint-disable no-new */

const sinon = require('sinon')
const MicroRadioInternet = require('./micro-radio-internet')

describe('MicroRadioInternet', () => {
  it('constructs', () => {
    new MicroRadioInternet()
  })
  it('needs more testing')
})
